# Copy to .env and fill in. Used by docker-compose / Coolify.

# Anthropic API key for the generator agent (backend only).
ANTHROPIC_API_KEY=sk-ant-...

# Postgres credentials (the backend's DATABASE_URL is derived from these).
POSTGRES_USER=processfox
POSTGRES_PASSWORD=change-me-please
POSTGRES_DB=processfox
